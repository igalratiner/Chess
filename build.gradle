subprojects {
    afterEvaluate {
        repositories {
            mavenLocal()
            mavenCentral()
            jcenter() // dependencies are fetched from the Bintray’s JCenter Maven repository
        }
    }
}