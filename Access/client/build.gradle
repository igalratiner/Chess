version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile project(':accounts:client')
    compile project(':utils:http')

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
